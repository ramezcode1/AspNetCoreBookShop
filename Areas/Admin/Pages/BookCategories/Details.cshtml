@page
@model BookStore.Areas.Admin.Pages.BookCategories.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>BookCategory</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.BookCategory.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.BookCategory.Name)
        </dd>
    </dl>
</div>
<div class="d-flex mt-2">
    <a class="btn btn-success me-1" asp-page="./Edit" asp-route-id="@Model.BookCategory?.Id">Edit</a>
    <a class="btn btn-secondary me-1" asp-page="./Index"><i class="bi bi-reply-all"></i> Back to List</a>
</div>
